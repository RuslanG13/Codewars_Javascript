/**
 * Declare two variables and name them first and second.
Write any number into the variable first.
Write any number into the variable second.
If first > second print ‘first won’ to the console.
If second > first print ‘second won’ to the console.
If first = second print ‘draw’ to the console.
 */

const first = 13;
const second = 9;

if (first > second) {
    console.log("first won");
} else if (first < second) {
    console.log("second won");
} else if (first === second) {
    console.log("draw");
}
/**
 * Declare a variable and name it what. (do you remember which data type a variable has if it's empty/not initialized?)
Check if what equal to that data type and print “Variable is not initialized yet” to the console
Else print “Variable is already initialized

 */

let what = 4;

if (what === undefined) {
    console.log("Variable is mot initialized yet");
} else {
    console.log("Variable is already initialized");
}
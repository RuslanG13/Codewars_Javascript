/**
 * Declare two variables - a and b, and create function multiply():
The function should multiply a and b and return the result.
Print what the function returns to the console.
Example: a = 5, b = 6, 30 should be printed to the console.
 */

let a = 5;
let b = 6;

function multiply() {
    return a * b;
}

console.log(multiply());
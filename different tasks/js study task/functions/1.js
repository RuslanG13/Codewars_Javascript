/**
 Declare three variables - a, b, res, and create function sum():
The function should add a plus b and assign the result into res variable.
Call the function
Print res variable to console
Example: a = 5, b = 6, 11 should be printed to the console.
 */

let a = 5;
let b = 6;
let res;

function sum() {
    res = a + b;
}

sum();

console.log(res);
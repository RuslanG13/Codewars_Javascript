/**
 * Create function combine() which accepts two arguments - string1 and string2:
The function should concatenate string1, space, and string2, and return the result.
Print what the function returns to the console.
Example: string1 = ‘hello’, string2 = ‘world’, ‘hello world’ should be printed to the console.

 */

function combine(string1, string2) {
    return string1 + " " + string2;
}

console.log(combine("bad", "boy"));